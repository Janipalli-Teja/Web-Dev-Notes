const express=require("express");

const { login_model, signup_model } =require("../login");
const { route } = require("./user");

const router=express.Router();

router.get("/login",(req,res)=>{

});
